apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: simple-vm-infra-aws
  title: Create vm on aws
  description: Create a new vm on aws
  tags:
    - aws
    - terraform
spec:
  owner: dpogula
  type: infrastructure
  parameters:
    - title: Provide some simple information
      required:
        - region
        - owner
      properties:
        region:
          title: Name
          type: string
          description: Unique name of the component
          ui:field: EntityNamePicker

        owner:
          title: Owner
          type: string
          description: Owner of the component
          ui:field: OwnerPicker
          ui:options:
            allowedKinds:
              - Group
    - title: Choose a location
      required:
        - repoUrl
      properties:
        repoUrl:
          title: Repository Location
          type: string
          ui:field: RepoUrlPicker
          ui:options:
            allowedHosts:
              - github.com
  steps:
    - id: terraform-init
      name: Terraform Init
      action: terraforminit:init
      command: |
        cd ${{ input.workDir }}/terraform
        terraform init
    - id: terraform-plan
      name: Terraform Plan
      action: terraformplan:plan
      command: |
        cd ${{ input.workDir }}/terraform
        terraform plan -var="region=${{ input.parameters.region }}"
    - id: terraformapply:apply
      name: Terraform Apply
      action: terraform:template
      command: |
        cd ${{ input.workDir }}/terraform
        terraform apply -auto-approve -var="region=${{ input.parameters.region }}"

  output:
    links:
      - title: Repository
        url: ${{ steps.publish.output.remoteUrl }}
      - title: Open in catalog
        icon: catalog
        entityRef: ${{ steps.register.output.entityRef }}
